package state

import _root_.state.machine.{Coin, Machine, Turn}
import org.scalatest.{FlatSpec, Matchers}

class MachineTest extends FlatSpec with Matchers {

//  "Machine" should "" in {
//    val machine = Machine.simulateMachine(List(Coin, Turn, Coin, Turn, Coin, Turn, Coin, Turn))
//
//    machine.run(Machine(true, 10, 5))._1 should equal(14, 1)
//    cats.data.State((s:Int) => (s, 5)).ru
//  }

}
